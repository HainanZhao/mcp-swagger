{
  "mcpServers": {
    "swagger-api": {
      "command": "mcp-swagger",
      "args": [
        "--doc-url",
        "https://example.com/swagger.json",
        "--tool-prefix",
        "myapi",
        "--ignore-ssl",
        "--auth-header",
        "Bearer token"
      ]
    },
    "local-swagger": {
      "command": "mcp-swagger",
      "args": [
        "--doc-file",
        "/path/to/swagger.json",
        "--tool-prefix",
        "local",
        "--base-url",
        "http://localhost:8080"
      ]
    }
  }
}
